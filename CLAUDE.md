Deploy: Google Cloud Run

Access control: HTTP(S) Load Balancer + IAP (allow only specific emails / Google Group)

Frontend: Next.js (TypeScript)

Backend: FastAPI (uv, pydantic v2, pydanticAI)

Chat: SSE streaming endpoint

DB: PostgreSQL (Cloud SQL in prod)

Do not add extra frameworks/services without updating docs/spec.md