# Debug mode (allows X-Debug-Email header)
DEBUG=true

# Comma-separated list of allowed emails (used in production when DEBUG=false)
ALLOWLIST_EMAILS=user1@example.com,user2@example.com

# Anthropic API key for chat functionality
ANTHROPIC_API_KEY=sk-ant-xxx

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000

# PostgreSQL database URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nurse_exam
